<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>College Details</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="text-center">College Details</h1>
      <div class="row">
        <% clg.forEach(college => { %>
        <div class="col-md-4">
          <div class="card mt-4">
            <img
              src="<%= college.url %>"
              class="card-img-top"
              alt="College Image"
            />
            <div class="card-body">
              <h3 class="card-title"><%= college.title %></h3>
              <p class="card-text"><%= college.description %></p>
              <p><strong>Fees:</strong> ₹<%= college.fees %></p>
              <p>
                <strong>Location:</strong> <%= college.state %>, <%=
                college.country %>
              </p>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
    </div>
  </body>
</html>
